{
  "name": "gloomdelve",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "sim": "esbuild sim/sim.ts --bundle --format=cjs --platform=node --outfile=.sim.cjs --log-level=error && node .sim.cjs"
  },
  "devDependencies": {
    "typescript": "~5.6.3",
    "vite": "^6.0.0"
  }
}